# Runtime dependencies for wood-quality-assessment
# Mirrors [project].dependencies in pyproject.toml
requests>=2.31.0
feedparser>=6.0.10
python-dateutil>=2.8.2
jsonschema>=4.20.0
PyYAML>=6.0.1

# Optional: full crawl4ai-based crawl backend (pyproject optional-extra "crawl")
# crawl4ai>=0.3.0
